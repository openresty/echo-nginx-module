WARNING!!! This module is still under initial development and is not ready for use at all.

Description

An nginx module for bringing the power of sh's "echo" command to Nginx's config file.

Installation

Grab the nginx source code from http://nginx.net/ , for example,
the version 0.7.62 and then build the source with this module:

    $ wget 'http://sysoev.ru/nginx/nginx-0.7.62.tar.gz'
    $ tar -xzvf nginx-0.7.62.tar.gz
    $ cd nginx-0.7.62/
    $ ./configure --prefix /opt/nginx \
        --add-module=/path/to/echo-nginx-module

Configuration

This is a sample configuration snippet in your nginx.conf file:

    http {
        ...
        server {
            ...
            location /echoback {
                echo_client_request_header;
            }
            location /hello {
                echo 'hello, world';
            }
            ...
        }
        ...
    }

As you can see, the only directive provided by this module is
"echo".

After starting the nginx server, one can query it this way:

    $ curl 'http://localhost/echoback'
    GET /echo HTTP/1.1
    User-Agent: curl/7.18.2 (i486-pc-linux-gnu) libcurl/7.18.2 OpenSSL/0.9.8g zlib/1.2.3.3 libidn/1.10
    Host: localhost
    Accept: */*

    $ curl 'http://localhost/hello'
    hello, world

Contact the author by agentzh at gmail dot com whenever you have
any questions.

